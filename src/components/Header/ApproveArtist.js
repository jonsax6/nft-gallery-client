import React, { useState, useEffect } from 'react'
import Web3 from 'web3'
import artists from '../../api/artists'

const ApproveArtist = () => {
  return (
    <div style={{ paddingTop: '100px', color: 'black' }}>In approve artist</div>
  )
}

export default ApproveArtist
